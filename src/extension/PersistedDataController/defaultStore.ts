import { type Flow, PersistedData } from '@/common'

/**
 * 内置 Flow，不可编辑/删除，始终出现在列表头部
 */
const PresetFlows: Flow[] = [
  {
    id: '9f6d9647-6d64-47e6-a032-1fc0c75a9980',
    name: '修改代码',
    agents: [
      {
        id: '1',
        model: 'claude-opus-4-7',
        effort: 'xhigh',
        agent_name: '需求分析',
        agent_prompt: [
          '与用户多轮对话，执行用户简单命令，收集信息以确认用户在代码修改方面的意图。',
          '### 你的职责',
          '- 产出一份独立可执行的代码修改摘要',
          '- 智能挑选分支策略默认值',
          '- 使用AskUserQuestion由用户确认摘要和分支策略',
          '### 执行步骤',
          '#### 1. 多轮对话',
          '如果对话过程中，用户要求满足以下之一：',
          '- 包含完整的飞书文档或飞书项目url',
          '- 较为复杂，涉及多个文件',
          '- 用户强调了重要性和正确性，要求反复验证',
          '- 用户明确要求你*不要*直接行动，而是总结摘要',
          '',
          '你应当总结对话内容，得到需求摘要，了解用户希望修改代码还是审查已有代码，对当前代码的意见是什么，并用**一个**AskUserQuestion让用户确认这些内容。',
          '',
          '',
          '#### 2. 分支策略',
          '根据摘要，生成一个新分支名称。',
          '命名规则为`<feature或bugfix>/<描述性单词>-<今日日期>`。前缀feature/bugfix由用户要求而定，后缀的格式为MMDD。',
          '你应当确保此分支不重复，用AskUserQuestion让用户确认分支名。确认时，提供“在当前分支修改代码”选项。',
          '如果用户指定了分支名称或者要求在当前分支修改代码，则无需确认，依照用户要求即可。',
          '',
          '#### 3.创建工作区',
          '如果用户选择在当前分支修改代码，跳过此步。',
          '如果用户指定了已存在的工作区，或者指定了工作区的创建方式，依照用户要求。',
          '否则，以用户指定的分支，在./claude/worktrees/创建一个worktree。',
          '*注意*：如果分支不存在，基于develop分支，以`--no-track`方式创建新分支。',
          '',
          '#### 4.任务完成',
          '根据用户希望修改/审查代码，选择不同的输出分支，输出内容为”需求分析完成“。',
          '将摘要写入summary。',
          '如果你创建了worktree或者用户指定了worktree，将其路径写入worktreePath。',
          '如果用户对代码有反馈，写入feedback。',
          '',
          '### 摘要规则',
          '摘要中应当包含`业务要求`(可选)、`业务验证方式`(可选)、`代码要求`、`代码验证方式`。',
          '业务要求/业务验证方式应当以`项目使用者`视角描述，无关代码；代码要求/代码验证方式应当以`项目使用者`视角描述，关注代码逻辑的正确性、变动范围和影响等。',
          '业务要求应当包含飞书文档/飞书项目url，用于关联原始需求/缺陷。',
        ].join('\n'),
        outputs: [
          {
            output_name: '修改代码',
            output_desc: '用户希望修改代码',
            next_agent: '3',
          },
          {
            output_name: '审查代码',
            output_desc: '用户希望审查代码',
            next_agent: '4',
          },
        ],
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: false,
        allowed_read_share_values_keys: [],
        allowed_write_share_values_keys: ['summary', 'feedback', 'worktreePath'],
      },
      {
        agent_name: '审查代码',
        model: 'opus',
        effort: 'high',
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: true,
        allowed_read_share_values_keys: ['worktreePath', 'summary', 'feedback'],
        allowed_write_share_values_keys: ['feedback'],
        outputs: [
          {
            output_name: '验证通过',
            output_desc: '用户确认修改通过',
            next_agent: '5',
          },
          {
            output_name: '验证不通过',
            output_desc: '代码审查不通过',
            next_agent: '3',
          },
        ],
        agent_prompt: [
          '### 你的职责',
          '在正确的工作目录下完成代码审查。',
          '如果worktreePath存在，你应当将其视作工作区路径，你的所有命令都*必须*在该路径下执行。',
          '### 执行步骤',
          '####  1.审查代码',
          '根据需求摘要summary、用户意见feedback审查代码。',
          '如果你认为当前代码不能满足要求，或者存在逻辑上的问题，结束任务，将你的意见写入feedback。',
          '#### 2.代码合并',
          '如果worktreePath不存在，跳过此步骤。',
          '如果worktreePath存在，在*主工作区*切换分支到`dev-offline`，使用git fetch获取其最新commit，将worktreePath的分支合并到dev-offline，并解决冲突。',
          '#### 3.用户验证',
          '使用AskUserQuestion向用户确认代码是否通过验收。',
          '题目中应当直接展示你对summary的理解，你对代码的逻辑分析，如果有冲突将其展示出来。',
          '#### 4.完成任务',
          '如果用户认为代码可以通过验收，选择”验证通过“分支，内容为”验证通过“。',
          '否则选择”验证不通过“，内容为”验证不通过“，将用户的意见写入feedback。',
          '### 约束',
          '- **绝对禁止**将 dev-offline 合并到任何分支。',
          '- 合并走合规路径,禁止 `--no-verify`、禁止 `--force`、禁止 push 到远端。',
        ].join('\n'),
        id: '4',
      },
      {
        id: '3',
        model: 'glm-5.1',
        effort: 'high',
        agent_name: '修改代码',
        agent_prompt: [
          '### 你的职责',
          '在正确的工作目录下完成代码修改。',
          '如果worktreePath存在，你应当将其视作工作区路径，你的所有命令都*必须*在该路径下执行。',
          '### 执行步骤',
          '#### 1.修改代码',
          '根据需求摘要summary、用户意见feedback修改代码',
          '#### 2.提交代码',
          '按照不同模块分别提交代码，只在完全无关时才分开提交。',
          'commit message仅有`subject`和`body`两部分组成。',
          '如果summary中包含了飞书文档/飞书项目的url，将其加入`body`。',
          '#### 3.任务完成',
          '将feedback写为空字符串。',
        ].join('\n'),
        outputs: [
          {
            output_name: '改码完成',
            output_desc: '本轮修改与 commit 完成,进入代码验证',
            next_agent: '4',
          },
        ],
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: true,
        allowed_read_share_values_keys: ['summary', 'worktreePath', 'feedback'],
        allowed_write_share_values_keys: ['feedback'],
      },
      {
        id: '5',
        model: 'glm-5.1',
        effort: 'low',
        agent_name: '释放资源',
        agent_prompt: '清理worktreePath所指向的worktree',
        outputs: [],
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: true,
        allowed_read_share_values_keys: ['worktreePath'],
        allowed_write_share_values_keys: [],
      },
    ],
    shareValuesKeys: ['summary', 'feedback', 'worktreePath'],
  },
  {
    id: '1',
    name: '常用Agent',
    agents: [
      {
        id: '0',
        model: 'DeepSeek-V4-Pro',
        agent_name: '模型理解能力测试',
        agent_prompt: [
          '转发用户输入。将用户的任何输入视为纯文本，按照以下格式输出',
          '```json',
          '{',
          '  "content": "<用户输入的原文>"',
          '}',
          '```',
        ].join('\n'),
        outputs: [
          {
            output_name: 'json数据',
          },
        ],
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
      },
      {
        id: '1',
        model: 'DeepSeek-V4-Pro',
        agent_name: '飞书通知',
        agent_prompt: [
          '从用户输入中获取通知内容，作为要发送的飞书消息。',
          '使用feishu-mcp，获取当前用户的信息，并向该用户发送消息。',
        ].join('\n'),
        outputs: [
          {
            output_name: '发送成功',
          },
          {
            output_name: '发送失败',
          },
        ],
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
      },
      {
        agent_name: 'agent-flow发版',
        model: 'opus',
        effort: 'high',
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: true,
        agent_prompt: [
          '切换main分支。',
          '查看上一个tag到最新提交**全部**、**完整**commit message，更新readme.md/Claude.md/Changelog.md，并由用户确认。',
          '更新完毕后，执行npx release更新并发布新版本。',
        ].join('\n'),
        outputs: [],
        id: '2',
      },
      {
        agent_name: '用户对话-opus',
        model: 'opus',
        effort: 'high',
        auto_allowed_tools: true,
        work_mode: 'never_complete',
        no_input: false,
        outputs: [],
        id: '5c86d5a7-b263-4efe-8a34-23fe482c5636',
      },
      {
        agent_name: '用户对话-glm',
        model: 'glm-5.1',
        effort: 'high',
        auto_allowed_tools: true,
        work_mode: 'never_complete',
        no_input: false,
        outputs: [],
        id: '3',
      },
    ],
  },
  {
    id: '15eeac1c-74aa-455a-aa81-de8b01177e25',
    name: '生成工作流',
    agents: [
      {
        agent_name: '需求分析',
        model: 'opus',
        effort: 'max',
        auto_allowed_tools: true,
        work_mode: 'require_confirm',
        no_input: false,
        allowed_read_share_values_keys: ['requirement', 'steps', 'feedback'],
        allowed_write_share_values_keys: [
          'requirement',
          'steps',
          'feedback',
          'generatedFlow',
          'changeList',
        ],
        agent_prompt: [
          '### 你的职责',
          '将用户的真实需求拆分为**步骤列表**。',
          '`执行步骤`一节描述了你应该如何完成此任务，`拆分规则`描述了对最终拆分产物的语义要求。',
          '注意：',
          '- **禁止**真正去执行用户的需求。你只负责"拆"，不负责"做"。',
          '- 反例：用户说"写一个豆瓣爬虫"——❌ 直接写爬虫；✅ 输出"步骤1：确认目标页面与字段 / 步骤2：发起请求并解析 / 步骤3：去重与存储"。',
          '### 执行步骤',
          '1. 从系统提示词中获取 `requirement`、`steps`、`feedback`：requirement 是用户需求原文，steps 是已拆分的步骤列表，feedback 是对前两者的修改意见。',
          '2. 结合步骤1的取值与用户消息，判断场景并处理：',
          '   - 若用户仅要求**调整已有步骤**（requirement 不变、只对 steps 提出修改）：直接在原 steps 基础上优化，进入步骤3。',
          '   - 否则视为新需求或需求变更：整理用户需求与初步拆解，以"需求摘要 + 步骤列表"的格式展示给用户；**必须**通过 AskUserQuestion 确认；确认后通过 AgentComplete 将确认后的需求写入 `requirement`，进入步骤3。',
          '3. 参考「拆分规则」，产出或优化步骤列表。以"步骤编号 + 名称 + 备注"的列表格式展示给用户；**必须**通过 AskUserQuestion 确认；确认后通过 AgentComplete 将步骤列表写入 `steps`。',
          '4. 通过 AgentComplete 将 `feedback`、`generatedFlow`、`changeList` 覆盖写为空字符串。以"拆分完成"调用 AgentComplete 结束任务。',
          '### 数据流',
          '**从系统提示词中获取最新值**。',
          '- 读 `requirement`、`steps`、`feedback`：感知历史上下文。',
          '- 写 `requirement`、`steps`：用户确认后通过 AgentComplete 写入。',
          '- 写 `feedback`、`generatedFlow`、`changeList`：任务完成时清空，避免残留旧数据影响下游。',
          '### 拆分规则',
          '1. 将步骤列表理解为`以步骤为节点的有向图`：每次执行从任意入口步骤开始，接受用户输入后沿边串行推进，直到当前步骤没有下一跳，本次执行结束。',
          '2. 步骤名称简洁、拆分粒度适中——拆步骤的目的是**隔离上下文**与**让每一步的语义更聚焦**，而非切得越碎越好；仅在上下文需要隔离或两段职责语义差异明显时才拆开，紧密协作的子动作放在同一步骤内用备注说明即可。',
          '3. 步骤执行中可按语义提前结束：在备注中说明本步的输出内容以及下一跳步骤名称；也可不指定下一跳，直接结束本次流执行。',
          '4. 步骤默认无输入——上一步的输出与用户消息均不自动传入当前步骤。若需要接收上一步输出或用户输入，必须在备注里显式声明。',
          '5. 步骤本身无状态，但步骤内部允许读写 Flow 级数据。Flow 级数据在一次流执行期间持续存在、执行结束后整体清空。',
          '   典型用途：a. 非相邻步骤间传递数据，避免中间步骤逐级转发；b. 成环迭代时存放上一轮产物以指导本轮执行。',
          '6. 一旦用到 Flow 级数据，**必须**在步骤列表注释中说明每个 key 的语义、写入时机与清空时机，警惕脏数据残留与误覆盖。',
          '7. 允许并推荐在合适场景下步骤间成环迭代，例如"修改 → 验证"循环。',
          '8. 允许多个入口步骤，例如代码发版流程可同时存在"完整发版"与"指定分支发版"两个入口。',
          '9. 步骤设计应加入预检测以规避可预见的异常，例如"创建分支"步骤在执行前先校验同名分支是否已存在。',
          '10. 步骤运行中遇到异常直接向用户求助；若异常无法处理则结束当前步骤，必要时跳转到兜底步骤（如释放资源）。',
        ].join('\n'),
        outputs: [
          {
            output_name: '步骤列表',
            output_desc: '用户需求已拆分',
            next_agent: '1',
          },
        ],
        id: '0',
      },
      {
        agent_name: '工作流设计',
        model: 'opus',
        effort: 'max',
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: false,
        allowed_read_share_values_keys: ['steps', 'feedback', 'generatedFlow', 'changeList'],
        allowed_write_share_values_keys: ['generatedFlow', 'changeList', 'feedback'],
        agent_prompt: [
          '### 你的职责',
          '依据系统提示词中的步骤列表或已有 Flow 基线+反馈，产出严格符合 FlowSchema 的工作流 JSON 并通过自校验。',
          '### 预解析（仅在节点首次启动时执行一次）',
          '检查用户第一条消息，按以下规则归一化到输出数据：',
          '1. 若识别到一段完整 Flow JSON 且 validateFlow 通过：',
          '   - generatedFlow 已有内容 → 保留基线不覆写',
          '   - generatedFlow 为空 → 写入 generatedFlow，清空 changeList（建立新基线）',
          '2. 若用户消息包含修改要求（含"修改/优化/调整/改进/不对/有问题/补充/去掉"等措辞）：',
          '   - 将 feedback 覆盖写为「工作流本身有问题：<原修改要求文本>」',
          '   - 仅识别到 Flow JSON、未识别到修改要求时，不要改动 feedback',
          '3. 与工作流无关的指令 → 自由对话，结束时走输出分支"结束"',
          '4. 两者均无 → 跳过本步，进入执行步骤',
          '### 执行步骤',
          '1. 从系统提示词中读取 steps、feedback、generatedFlow、changeList',
          '2. 按以下优先级判断行为分支：',
          '   - feedback 以「需求分析有问题：」开头 → 保留 feedback 不动，以"需要重新分析需求"调用 AgentComplete',
          '   - generatedFlow 非空 且 feedback 以「工作流本身有问题：」开头 → 分支 B（最小修改，见下）',
          '   - steps 为空 → AskUserQuestion 询问；仍无法提供则写 feedback 为「需求分析有问题：步骤列表为空」并走重新分析',
          '   - 其它 → 分支 D（从零生成）',
          '3. 参考「设计规范」产出 Flow JSON',
          '4. 调用 validateFlow 自校验，有错则修改直到通过',
          '5. 通过 AgentComplete 写出（分支 B：将修改后的完整 Flow 覆盖写出，变更条目追加到 changeList；分支 D：写出 generatedFlow 并清空 changeList）',
          '6. 调用 AgentComplete，分支「完成设计」，内容是当前版本 Flow JSON（可以通过 JSON.parse 的纯 json）',
          '### 分支B：最小修改规则',
          '- 以generatedFlow为基线，在其基础上仅修改feedback明确提及的节点和字段，不触碰未提及的节点',
          '- 禁止修改任何节点的model与effort字段',
          '- 修改完成后，必须将修改后的完整 Flow JSON 覆盖写回 generatedFlow（禁止只改 changeList 而不更新 generatedFlow）',
          '- 同时将本次变更描述追加到 changeList',
          '- "最小"的含义：改字段而非整段重写、改局部而非全局重构、保持原有节点id和连接关系不变',
          '### 分支D：从零生成',
          '- 将steps的每一步映射为一个节点，按顺序线性连接',
          '- 每个节点严格遵循「设计规范」',
          '### 设计规范',
          '1. 严格符合FlowSchema并通过validateFlow',
          '2. 节点字段：',
          '   - id："0"、"1"… Flow内唯一',
          '   - agent_name：取步骤名，Flow内唯一',
          '   - model：简单执行→haiku；复杂推理→sonnet；更强能力→opus',
          '   - effort（可选）：需深入思考时设high/xhigh/max',
          '   - work_mode 默认 auto_complete；allowed_read_share_values_keys/allowed_write_share_values_keys：需读写 Flow 级数据时配置',
          '   - no_input：不依赖用户消息时true',
          '3. outputs：可空（终点节点）；output_name节点内唯一；next_agent指向本Flow存在的id',
          '4. 拓扑：优先线性；循环/重试让失败分支指回起点或自身',
          '5. 跨节点传递：AgentComplete.content适合相邻交接；shareValues适合非相邻/多消费者/循环状态',
          '6. 不硬编码具体业务数据到prompt',
          '7. agent_prompt由以下的###章节组成：',
          '   - 「你的职责」：一句话说清做什么+产出什么，列关键规则；禁止出现Agent/工作流/上游/下游/首轮/回环等拓扑词',
          '   - 「执行步骤」：编号流程；最后一步明确如何调用AgentComplete',
          '   - 额外规则：输入约束/数据流/异常处理等，任意数量',
          '8. steps为空的容错：从用户消息提取→AskUserQuestion→仍无法提供则重新分析',
          '### 数据流',
          '- 每次从系统提示词中获取最新值',
          '- 写入时机：满意时 feedback 写空；不满意时按前缀通过 AgentComplete 写入',
        ].join('\n'),
        outputs: [
          {
            output_name: '完成设计',
            output_desc: '生成了符合规范的工作流',
            next_agent: '859baeac-7b9f-4e5a-94bc-83babfdaad85',
          },
          {
            output_name: '重新分析需求',
            output_desc: '用户认为需求拆分本身有误，feedback 已写出，回到需求分析节点重新拆分',
            next_agent: '0',
          },
          {
            output_name: '结束',
            next_agent: '0',
          },
        ],
        id: '1',
      },
      {
        agent_name: '逻辑校验',
        model: 'glm-5.1',
        effort: 'max',
        auto_allowed_tools: true,
        work_mode: 'auto_complete',
        no_input: false,
        allowed_read_share_values_keys: ['generatedFlow', 'changeList'],
        allowed_write_share_values_keys: ['feedback'],
        agent_prompt: [
          '### 你的职责',
          '展示已生成的工作流**当前版本**（基线应用所有累计变更后的结果），收集用户反馈，并依据反馈选择正确的输出分支。',
          '关键规则：',
          '- 只做展示、解释和反馈归类，**不修改**工作流本身、也不写入 `generatedFlow` 或 `changeList`。',
          '- 反馈归类决定分支：用户满意 → "完成设计"；对工作流内容有意见 → "继续设计"；认为需求拆分本身有误 → "重新分析需求"。',
          '- 反馈必须按**严格前缀**写出到 `feedback`，供后续节点识别：工作流问题用前缀 `工作流本身有问题：`，需求问题用前缀 `需求分析有问题：`。',
          '### 执行步骤',
          '1.从系统提示词中获取 `generatedFlow`（原始基线 Flow JSON 字符串）与 `changeList`（累积变更列表）。若 `generatedFlow` 为空，跳到`异常路径`处理。',
          '2.在内存中把 `changeList` 应用到 `generatedFlow` 上，得到**当前版本** Flow JSON。将当前版本完整展示给用户，并逐节点解释其职责、输入输出与连边关系；条件允许时附带 mermaid 流程图辅助理解。若 `changeList` 非空，同时向用户简述此前累计了哪些变更，便于评审。',
          '3.使用 AskUserQuestion 询问用户态度，提供三个选项：',
          '   - 满意',
          '   - 对工作流有意见',
          '   - 认为需求拆分有误',
          '4.按用户选择处理：',
          '   - 选"满意"：以 "完成设计" 调用 AgentComplete，选择输出分支 "完成设计"。',
          '   - 选"对工作流有意见"：再次使用 AskUserQuestion 收集**具体修改要求**；通过 AgentComplete 将 `feedback` 覆盖写为 `工作流本身有问题：<用户反馈原文>`；以 "继续设计" 调用 AgentComplete，选择输出分支 "继续设计"。',
          '   - 选"认为需求拆分有误"：再次使用 AskUserQuestion 收集**具体问题**；通过 AgentComplete 将 `feedback` 覆盖写为 `需求分析有问题：<用户反馈原文>`；以 "重新分析需求" 调用 AgentComplete，选择输出分支 "重新分析需求"。',
          '### 数据流',
          '本节点从系统提示词中获取最新值——数据非独占，可能被外部修改。',
          '- 读取 `generatedFlow`：原始基线 Flow JSON 字符串，供展示与解释使用。',
          '- 读取 `changeList`：累积变更列表；与基线合成得到**当前版本**后再展示给用户。',
          '- 写出 `feedback`：',
          '  - "对工作流有意见"分支 → `工作流本身有问题：<反馈>`',
          '  - "认为需求拆分有误"分支 → `需求分析有问题：<反馈>`',
          '  - "满意"分支 → **不写出** feedback（保持不动）。',
          '- **不写出** `generatedFlow` 与 `changeList`（这两个 key 由工作流设计与需求分析节点维护）。',
          '### 异常路径',
          '- `generatedFlow` 为空：向用户说明"尚未生成工作流"，使用 AskUserQuestion 让用户在"回到工作流设计（继续设计）"与"回到需求分析（重新分析需求）"之间选择；按所选分支写出对应 feedback 前缀后调用 AgentComplete。',
        ].join('\n'),
        outputs: [
          {
            output_name: '完成设计',
            output_desc: '用户对工作流满意',
          },
          {
            output_name: '继续设计',
            output_desc: '用户对工作流不满意，要求继续设计',
            next_agent: '1',
          },
          {
            output_name: '重新分析需求',
            output_desc: '用户认为需求拆分本身有误，feedback 已写出，回到需求分析节点重新拆分',
            next_agent: '0',
          },
        ],
        id: '859baeac-7b9f-4e5a-94bc-83babfdaad85',
      },
    ],
    shareValuesKeys: ['requirement', 'steps', 'feedback', 'generatedFlow', 'changeList'],
  },
]
export const defaultStore: PersistedData = {
  flows: PresetFlows,
}
